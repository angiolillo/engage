package ui.queue;

import javax.swing.ImageIcon;

import backend.library.MediaFile;
import backend.library.ThumbLabel;
 
public class QueueImage extends ThumbLabel {
 
            //private MediaFile mFile;
            
            //Index in MediaStrip
            private int index;
            
            public QueueImage(ImageIcon icon) {
            	super(icon);
            }
            
            public QueueImage(MediaFile mFile) {
            	super(mFile,true);
            	//this.mFile = mFile;
            }
            
            public void setQueueIndex(int i) {
            	index = i;
            }
            
            public int getQueueIndex() {
            	return index;
            }
           
}